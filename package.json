{
  "author": "edwin@mediamonks.com",
  "licence": "MIT",  
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "analyse": "yarn lint",
    "clean": "rimraf .build .serverless",
    "prebuild": "yarn clean && yarn analyse",
    "build": "serverless package",
    "predeploy": "yarn clean && yarn analyse",
    "deploy": "serverless deploy",
    "deploy:init": "serverless create_domain",
    "lint": "tslint --project . --format verbose",
    "start": "serverless offline start"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.53",
    "@types/node": "^14.0.11",
    "aws-lambda": "^1.0.6",
    "aws-sdk": "^2.691.0",
    "rimraf": "^3.0.2",
    "serverless": "^1.72.0",
    "serverless-plugin-typescript": "^1.1.9",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  }
}
